当前的编辑器非常的简陋，为了提升用户体验，需要支持以下功能：
* SQL自动补全系统：
  * ，允许读取连接信息，如表名、字段名、常用语句等进行补全。
* CTRL+enter 自动执行当前鼠标光标所在区域的完整语句.
  * 确定范围：如果用户选中了代码，则优先执行选中的部分；如果没有任何选中，才去自动检测光标所在的语句
  * 捕获按键（监听快捷键）：当检测到 Ctrl 和 Enter 同时被按下时，且当前是编辑器内，触发对应的处理逻辑
  * 执行目标代码：将SQL语句发送给执行器执行，并获取和展示结果
